<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jest.smart">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".AnimalDisease"
            android:label="@string/animal_disease" />
        <activity
            android:name=".PlantDisease"
            android:label="@string/plant_disease" />
        <activity
            android:name=".PoultryDisease"
            android:label="@string/poultry_disease" />
        <activity android:name=".AnimalDetail" />
        <activity android:name=".PlantDetail" />
        <activity android:name=".PoultryDetail" />
        <activity
            android:name=".About"
            android:label="@string/about" />
        <activity
            android:name=".Feedback"
            android:label="Feedback" />
        <activity
            android:name=".WelcomeScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>